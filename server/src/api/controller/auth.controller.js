export default {
    signUp : (req , res , next) => {
        //implement signup
         //write the actual function in the service not here
        //here you should only do validation
        
        /**
         * It should follow similar structure
         * 
         * 1 ->  validation
         * 2 -> call the appropriate service
         * 
         */
    },
    signIn : (req , res , next) => {
        //implement signin
         //write the actual function in the service not here
        //here you should only do validation
    },
    changePassword : (req , res , next) => {
        //implement change passowrd
        //write the actual function in the service not here
        //here you should only do validation
    }
}